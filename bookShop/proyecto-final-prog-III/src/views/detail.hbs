<div style="height: 50px;"></div>
<div class="container mt-5">
    <div class="card shadow-sm p-4">
        <h1 class="mb-4">Details</h1>
        <div class="row">
            <div class="col-md-4">
                <img src="{{book.image}}" class="img-fluid rounded" alt="{{book.title}}">
            </div>
            <div class="col-md-8">
                <h2>{{book.title}}</h2>
                <p><strong>Publication Year:</strong> {{book.publicationYear}}</p>
                <p><strong>Category:</strong> {{book.category.name}}</p>
                <p><strong>Author:</strong> {{book.author.name}}</p>
                <p><strong>Publisher:</strong> {{book.publisher.name}}</p>
                <p><strong>Publisher Country:</strong> {{book.publisher.country}}</p>
                <p><strong>Publisher Phone:</strong> {{book.publisher.telephone}}</p>
            </div>
        </div>
        <div class="mt-4">
            <a href="/" class="btn btn-secondary">Back</a>
        </div>
    </div>
</div>
<div style="height: 50px;"></div>